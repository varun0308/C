#include<stdio.h>

int leap_years(int day,int month,int year)
{
    int leap_year_inbetween,i;
    for(i=year;i<year1;i++)
    {
        if(i%4 == 0 && i%4 != 0)
        {
            leap_year_inbetween += 1 ;
        }
    }
}
int main()
{
    int day,month,year,day1,month1,year1;
    scanf("%d %d %d",&day,&month,&year);
    scanf("%d %d %d",&day1,&month1,&year1);
    

    
    return 0;
}